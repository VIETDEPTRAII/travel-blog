<% if object.errors.any? %>
  <h2>The process in <%= object.class.name.downcase %> form has some errors:</h2>
  <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
