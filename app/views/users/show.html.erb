<h1 class="text-center mt-4"><%= @user.username %></h1>
<%= gravatar_for @user%>
<% if logged_in? && @user == current_user %>
  <h1 class="text-center mt-4"><%= link_to "Edit profile", edit_user_path(@user), class: "btn btn-outline-info" %></h1>
<% end %>
<h3 class="text-center mt-4 mb-4">Articles</h3>
<%= render "articles/articles" %>
